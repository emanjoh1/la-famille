version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm install
    build:
      commands:
        - env | grep -e NEXT_PUBLIC_ >> .env.production
        - env | grep -e CLERK_SECRET_KEY >> .env.production
        - env | grep -e CLERK_WEBHOOK_SECRET >> .env.production || true
        - env | grep -e SUPABASE_SERVICE_ROLE_KEY >> .env.production
        - env | grep -e STRIPE_SECRET_KEY >> .env.production
        - env | grep -e STRIPE_WEBHOOK_SECRET >> .env.production
        - env | grep -e UPLOADTHING_TOKEN >> .env.production || true
        - env | grep -e UPLOADTHING_SECRET >> .env.production || true
        - env | grep -e UPLOADTHING_APP_ID >> .env.production || true
        - env | grep -e RESEND_API_KEY >> .env.production
        - env | grep -e RESEND_FROM_EMAIL >> .env.production
        - env | grep -e FLW_PUBLIC_KEY >> .env.production
        - env | grep -e FLW_SECRET_KEY >> .env.production
        - env | grep -e FLUTTERWAVE_SECRET_HASH >> .env.production || true
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
